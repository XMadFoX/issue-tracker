# Unit And Component Tests

Place Vitest tests for app code here when the test does not naturally live beside the source file.

Use colocated `*.test.ts` or `*.test.tsx` files under `src` for narrow unit tests, and this directory for broader component tests that need shared test helpers.
