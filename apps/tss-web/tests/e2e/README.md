# End-To-End Tests

Playwright tests live here and use page objects from `tests/e2e/pages`.

Run e2e tests with:

```sh
bun -F tss-web test:e2e
```

Run the Playwright UI with:

```sh
bun -F tss-web test:e2e:ui
```
